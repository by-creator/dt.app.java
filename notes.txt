Commandes

mvn clean install

mvn spring-boot:run

git add . && git commit -m "update" && git push -u origin main

Je souhaite que lorsque l'on clique sur gfa admin que l'on arrive sur un écran composé d'un wizard avec ces onglets : 

Vue globale - Services - Guichets - Agents - Tickets - Paramètres - Ecran